language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-geni' ]; then cd .. && eval "mv $currentfolder generator-geni" && cd generator-geni; fi

